import React from "react";
import { Range } from "./Range";
import ReactDOM from 'react-dom/client';

// ReactDOM.render(<Range name="Taylor" />);

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Range />);